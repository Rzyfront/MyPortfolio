const initialState = {
  lenguage: "en",
  lenguagePack: {},
};

const rootReducer = (state = initialState, { type, payload }) => {
  switch (key) {
    case value:
      break;

    default:
      break;
  }
};
